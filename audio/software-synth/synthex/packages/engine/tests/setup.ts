// Vitest setup. Currently empty — kept as a placeholder so tests can grow
// into Web Audio polyfills (e.g. node-web-audio-api) if we later need to
// drive the worklet through a real OfflineAudioContext.
export {}
