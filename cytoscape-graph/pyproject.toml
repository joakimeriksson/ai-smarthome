[project]
name = "cytoscape-graph"
version = "0.1.0"
requires-python = ">=3.8"
description = "An interactive technology journey visualization with an in-browser editor."
dependencies = [
    "flask",
]

[tool.uv]
# This is an app, not an installable package — don't try to build/discover it.
package = false
